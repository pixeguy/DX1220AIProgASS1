#pragma once
	static const int m_noGrid = 20;
	static const float m_worldHeight = 100.f; //this is actually wrong, its 600. but its too late will change later
	static const float m_worldWidth = m_worldHeight;
	static const float m_gridSize = m_worldHeight / m_noGrid;
